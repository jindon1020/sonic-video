# AI 意图导向视频剪辑智能体设计方案 (Design Specification)

## 1. 核心愿景
开发一个“懂人心”的剪辑助手，用户只需甩入原始素材和音乐，并说出剪辑意图（如“假日vlog，轻松愉快”），智能体即可自动完成：**内容理解 -> 节奏卡点 -> 意境匹配 -> 自动剪辑**。

---

## 2. 核心技术细节 (重点讨论部分)

### 2.1 音频冲突博弈：分层决策模型
当歌词、节奏、情感在同一时间点不一致时，系统按照以下权重处理：
- **语义层 (歌词)**：解决“剪什么”。决定素材的搜索关键词。作为粗粒度划分。
- **结构层 (节奏)**：解决“何时剪”。决定物理切点 (Cut Points)，确保视频必须踩在鼓点或旋律转折上。
- **氛围层 (情感)**：解决“怎么剪”。决定滤镜、转场速度和慢动作权重。
- **策略**：
    - *词快节奏慢*：保持镜头稳定，通过画面内的微小缩放 (Zoom) 匹配词频。
    - *词慢节奏快*：在同一句歌词下进行“多镜头快剪”，切换不同角度的同类素材。

### 2.2 视频切片与索引：虚拟化与多帧采样
针对长视频素材，采用“轻量化”处理策略：
1. **虚拟切片 (Virtual Subclipping)**：系统不再物理切割成百上千个小 mp4 文件。改为建立“时间戳索引数据库”。在最后合成阶段由 FFmpeg 动态从原视频截取流，极大降低磁盘 I/O 和空间开销。
2. **多帧投影 (Multi-frame Aggregation)**：不再单凭“中间一帧”代表全片。对每个场景的 25%、50%、75% 位置分别提取关键帧，通过 **Mean Pooling (平均池化)** 生成一个代表该镜头完整动态的聚合向量。
3. **长度约束**：过滤 <1.0s 的片段；对 >10s 的静止片段强制执行滑动窗口分片，确保检索精度。

### 2.3 意向化映射与创意重构 (Creative Reframing)
当上传素材与意图完全不符时（例如：意图是“励志”，只有“吃饭”素材）：
- **语义桥接**：LLM 将“励志”重定义为“平凡生活中的坚持”，搜索“热气腾腾的火锅”、“夜晚的灯光”等隐喻性画面。
- **视觉翻译官**：LLM 不再直接搜“歌词原文”，而是将其翻译为 CLIP 可理解的**视觉描述词**（意境词+动作词+光影词）。

### 2.4 交互设计：可视化思维链 (Thinking UI)
通过记录 AI 的决策路径，让用户感知其“智能”：
- **感知日志**：展示视频库中有多少笑容、多少大场景、多少次转场。
- **决策解释**：如“*检测到歌词提到‘飞翔’但无直接素材，正在尝试用‘窗外流逝的光影’替代以营造速度感*”。
### 2.5 审美控制：视觉风格锚点 (Aesthetic Consistency)
为了解决 AI 剪辑“画质/色调不统一”的问题：
- **风格注入**：在 LLM (Qwen) 生成视觉建议时，强制注入全局风格锚点（如：*Cinematic, Professional lighting, Teak & Orange color grading*）。
- **色彩约束**：所有检索 Query 均会自动携带这些锚点词，确保从成百上千个电影镜头中选出的片段在色调和光影上高度趋同。
- **字幕渲染**：自动添加“电影感”动态字幕，配备半透明黑色背景遮罩，提升最终成品的高级感。

---

## 3. 技术系统实现

### 3.1 技术栈 (Python Ecosystem)
- **后端**: FastAPI + Redis (任务队列)
- **向量库**: **Milvus** (处理大规模视频帧特征)
- **多模态模型**: CLIP (特征提取) / GPT-4o 或 Gemini 1.5 (逻辑编剧)
- **视频引擎**: MoviePy (合成与渲染) + SceneDetect (切分)
- **音频引擎**: Whisper (歌词对齐) + Librosa (节奏分析)

### 3.2 向量检索逻辑
1. **多帧采样**：对场景进行 3 帧采样 -> 生成 3 个 CLIP Embeddings -> **Mean Pooling 聚合**。
2. **虚拟索引**：将聚合向量与时间戳索引关联。
3. **逻辑导演**：LLM 解析歌词 + 意图 -> 生成带**风格锚点词**的 Search Queries。
4. **智能去重 (De-duplication)**：扩大检索池至 `Top-20`，通过 `used_paths` 机制确保整首歌中相同镜头不重复出现，优先补齐视觉缺口。

---

## 4. 开发路标
- **V1 (甩手掌柜模式)**：实现“全自动一键出片”，跑通从切分到向量检索、MoviePy 合成的全闭环。
- **V2 (导演模式)**：允许用户对 LLM 生成的分镜脚本进行微调或指定特定片段，升级进度可视化。
